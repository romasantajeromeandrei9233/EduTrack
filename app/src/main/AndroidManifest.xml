<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduTrack">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Teacher Activities -->
        <activity
            android:name=".ui.teacher.TeacherDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.teacher.ClassDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.teacher.StudentDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.teacher.TeacherProfileActivity"
            android:exported="false" />

        <!-- Parent Activities -->
        <activity
            android:name=".ui.parent.ParentDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.parent.StudentAttendanceActivity"
            android:exported="false"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="OPEN_ATTENDANCE_ACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.parent.ParentProfileActivity"
            android:exported="false" />

        <!-- FCM Service -->
        <service
            android:name=".firebase.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>